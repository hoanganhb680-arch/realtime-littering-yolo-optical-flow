# Frontend

React + Vite dashboard for the trash violation detection system.

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run build
```

The development server proxies `/api` and `/ws` to the FastAPI backend at `http://127.0.0.1:8000`.

